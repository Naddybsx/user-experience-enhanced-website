{% render 'partials/head.liquid' %}
{% include 'partials/nav.liquid' %}

<header class="hero">
    <picture>
      <source type="image/avif" srcset="/assets/bib-hero2.avif">
      <source type="image/webp" srcset="/assets/bib-hero2.webp">
      <img src="/assets/bib-hero2.png" alt="" width="800" height="533">
    </picture>
    <h1>Stekjes</h1>
<a href="#intro" class="scroll-down" data-scroll-target="#intro">▼</a>
</header>

<main>   
 <section class="intro" id="intro">
    <div>
        <h2>Hoe werkt het?</h2>
        <p>
            Bij Buurtcampus Oost kun je de stekjeskast bezoeken om stekjes te ruilen. 
            Je kunt ook op onze website inzien welke stekjes beschikbaar zijn. 
            Dit is een geweldige manier voor plantenliefhebbers om met elkaar in contact te komen en planten te delen.
            Het maakt het gemakkelijk om je favoriete stekjes te vinden en te ruilen met anderen in de buurt, zonder gedoe.
            Kom langs bij Buurtcampus Oost en ontmoet mede-plantenliefhebbers!
        </p>
        <a href="#stekjes" class="button" data-scroll-target="#stekjes">Bekijk stekjes</a>
    </div>

   
  <div class="carousel-wrapper"> 
    <ul class="carousel-container">
        {% comment %} For loop om te itereren door de stekjeskast afbeeldingen data {% endcomment %}
      {% for afbeelding in afbeeldingen %}
        <li class="carousel-item">
          <picture>
            <source srcset="https://fdnd-agency.directus.app/assets/{{ afbeelding.foto.id }}?format=avif&width=800" type="image/avif">
            <source srcset="https://fdnd-agency.directus.app/assets/{{ afbeelding.foto.id }}?format=webp&width=800" type="image/webp">
            <img
              src="https://fdnd-agency.directus.app/assets/{{ afbeelding.foto.id }}?width=800"
              width="{{ afbeelding.foto.width }}"
              height="{{ afbeelding.foto.height }}"
              alt="Stekjeskast afbeelding"
              loading="lazy"
              style="background-color: #e3e0e0;"
              >
          </picture>
        </li>
      {% endfor %}
    </ul>

    <p class="scroll-hint">
        Scroll voor meer →
     </p>   
  </div>
 </section>

 <section class="stekjes-section" id="stekjes">
    <h2>Beschikbare stekjes</h2>
  
    <ul class="stekjes-carousel">
      {% for stekje in stekjes %}
        <li class="stekje-card">
          <picture>
            <source srcset="https://fdnd-agency.directus.app/assets/{{ stekje.foto.id }}?format=avif&width=600" type="image/avif">
            <source srcset="https://fdnd-agency.directus.app/assets/{{ stekje.foto.id }}?format=webp&width=600" type="image/webp">
            <img
              src="https://fdnd-agency.directus.app/assets/{{ stekje.foto.id }}?width=600"
              width="{{ stekje.foto.width }}"
              height="{{ stekje.foto.height }}"
              alt="{{ stekje.naam }}"
              loading="lazy"
              style="background-color: #e3e0e0;"
              >
          </picture>
            <h3>{{ stekje.naam }}</h3>
            <a href="/stekje/{{ stekje.id }}" class="button">Meer informatie</a>
        </li>
      {% endfor %}
    </ul>
  
    <!-- Carousel buttons (alleen zichtbaar met JS) -->
    <nav aria-label="Carrousel navigatie" class="carousel-nav" hidden>
      <button id="prev" aria-label="Vorige stekje">❮</button>
      <button id="next" aria-label="Volgende stekje">❯</button>
    </nav>
  </section>
  
</main>
{% render 'partials/foot.liquid' %}